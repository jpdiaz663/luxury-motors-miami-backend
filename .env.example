# smtp = PHPMailer SMTP (Gmail locally, Hostinger in production).
# php_mail = PHP mail() without SMTP.
LM_NOTIFY_TRANSPORT=smtp
LM_SMTP_HOST=smtp.gmail.com
LM_SMTP_PORT=587
LM_SMTP_ENCRYPTION=tls
LM_NOTIFY_FROM=
LM_SMTP_USERNAME=
LM_SMTP_PASSWORD=
